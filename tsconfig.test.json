{
    "extends": "./tsconfig.json",
    "exclude": [
        "angular-src", // <-- We removed the spec files in order to compile them as well
        "src/tests",
        "node_modules",
        "dist"
    ]
}